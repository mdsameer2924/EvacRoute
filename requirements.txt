# EVACROUTE — Python backend dependencies
#
# SQLite3 needs no separate driver: the sqlite3 module ships with CPython and
# Django's 'django.db.backends.sqlite3' engine uses it directly.
#
# Tested with Python 3.14 + Django 6.1.1 (any Django 5.2 LTS or newer works).

Django>=5.2,<7
